{
  "name": "aya",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --env-file=.env index.ts",
    "test": "bun --env-file=.env ./tests/index.ts"

  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "ws": "^8.16.0"
  }
}